{
  "name": "stellar-nft-issuer",
  "version": "0.1.0",
  "description": "a package for issuing non-fungible tokens on the Stellar network",
  "main": "lib/index.js",
  "bin": {
    "nft-federation-server": "bin/server.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel --presets env -d lib/ src/",
    "prepublish": "npm run compile"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "ipfs-api": "^18.2.0",
    "ipfs-merkle-dag": "^0.7.3",
    "ipfs-unixfs": "^0.1.14",
    "pg-promise": "^8.2.2",
    "stellar-sdk": "^0.8.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
